# Pixel Atelier

A pixel and sprite editor — frames, onion skin, palettes — that runs in the
browser with no install and no build step. Built on
[SACRVM APPKIT](https://github.com/SACRVM/sacrvm-appkit); runs standalone or
as an app on a SACRVM desktop.

*Work in progress.*

```bash
npx serve .
```
